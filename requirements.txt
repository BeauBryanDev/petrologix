# Aegis-Geo-Mind backend.

# The lithology model ships as a local wheel in wheels/ 
# Its version encodes the model version:
# petrologix 0.2.x == model v2. It pulls in xgboost/scikit-learn/pandas pinned to
# the versions the model artifact was built against.

# Install:  pip install -r requirements.txt
./wheels/petrologix-0.2.1-py3-none-any.whl

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27

# LAS parsing (petrologix declares this as an optional extra; the backend needs it
# because users upload .las files).
lasio>=0.31

anthropic>=0.39.0

# RAG. Query embeddings are an HTTP call (bge-large-en-v1.5, 1024-dim), so no
# torch or sentence-transformers here -- same reasoning as the LLM.
qdrant-client>=1.9
# langgraph
# langchain-core
langchain==1.3.14
langchain-core==1.5.3
langchain-protocol==0.0.18
langgraph==1.2.10
langgraph-checkpoint==4.2.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langsmith==0.10.17

# Tests (pytest.ini sets asyncio_mode=auto; run: ./geologs/bin/python -m pytest)
pytest>=8.0
pytest-asyncio>=0.23
